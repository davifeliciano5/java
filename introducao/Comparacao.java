package estudos.java.introducao;

public class Comparacao {
    /*
    Operador == e equals()
    O operador == é usado para comparar referências de objetos, ou seja, verifica se
    duas variáveis de referência apontam para o mesmo objeto na memória. Quando você usa
	== para comparar dois objetos em Java, você estáverificando se as referências
	(endereços de memória) apontam para o mesmo objeto, não se os objetos em si
	são iguais em termos  de conteúdo.
	O método equals() é usado para comparar o conteúdo dos objetos, não apenas as
	referências
     */
}
